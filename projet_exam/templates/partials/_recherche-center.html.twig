<div class="card text-center m-auto " style="box-shadow: 0 0 10px rgba(0, 0, 0, 0.15); background-color: #DCEEFB">
<h3 class="card-title text-start mb-1 mt-2 p-2"> <i class="fas fa-xs fa-search"></i>
    Recherche de centres
</h3>
    {{ form_start(form) }}
    <div class="d-flex flex-row card-text justify-content-between justify-content-around align-items-center p-2">

        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary mt-2">Rechercher</button>
    </div>
    {{ form_end(form) }}
</div>
